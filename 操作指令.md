# FRUS Agentic RAG 搭建操作指令（交給執行 Agent）

專案根目錄：`/home/haoche_nitro_v15/FRUS_agenticRAG`  
硬體：RTX 4060 Laptop 8GB、RAM 32GB、WSL2 Ubuntu  
目標：建立全已發布 FRUS 的雙語、可追溯、可評估 **Bounded Agentic RAG**，而不是把固定 RAG 包進聊天 Agent。

## 0. 結論與禁止事項

採用：**LangGraph + Adaptive Planning + Multi-hop Hybrid Retrieval + Corpus-only Corrective Retry + Canonical Citation Gate**。

```text
question
  → plan / route
      ├─ document lookup
      ├─ simple hybrid retrieval
      ├─ timeline retrieval
      ├─ series-status lookup
      └─ complex question → 2–3 subqueries
  → BM25 + BGE-M3 → RRF
  → evidence grade
      ├─ sufficient → synthesize
      ├─ missing → rewrite missing hop and retry once
      └─ exhausted → abstain
  → deterministic citation validation
      ├─ valid → answer
      └─ invalid → abstain
```

禁止：

- 不做無上限 ReAct loop；最多 3 個 subqueries、2 輪 retrieval、4 次 LLM calls。
- 不做 multi-agent supervisor；先證明單一 typed graph 有品質收益。
- 不提供一般 web search。FRUS 查不到就拒答，不用網路內容冒充 FRUS 證據。
- 不自稱完整重現 SELF-RAG、CRAG 或 Adaptive-RAG；本案是受研究啟發、獨立評估的工程圖。
- 不刪除 2-step RAG；它必須保留為 baseline，證明 agentic nodes 是否真的有用。
- 不修改 `jobshift:latest`、Jobshift 原始碼或資料；只衍生新 image，BGE-M3 cache 唯讀掛載。

## 1. 為何選 LangGraph

三個候選：

| 目標 | 候選 | 判定 |
|---|---|---|
| 最快跑通 | Ollama 原生 tool loop | 程式少，但 state、checkpoint、branch testing 與 trace 要自己補 |
| 成熟且好維護 | **LangGraph typed StateGraph** | **採用**；官方有 retrieve-grade-rewrite RAG 教程，支援 conditional edges、persistence、streaming 與測試 |
| 最強表演性 | multi-agent supervisor／research agents | 不採；4B 本機模型、多次推論與 agent handoff 會放大延遲與錯誤 |

使用 LangGraph 的 Graph API；tools、LanceDB 與 BGE-M3 保持普通 Python 函式，不必全部包成 LangChain VectorStore。

## 2. 先檢查環境與專案狀態

```bash
cd /home/haoche_nitro_v15/FRUS_agenticRAG
pwd
find . -maxdepth 2 -type f | sort
git status --short --branch 2>/dev/null || true
docker compose version
docker image inspect jobshift:latest >/dev/null
docker run --rm --gpus all --entrypoint nvidia-smi jobshift:latest
test -d /home/haoche_nitro_v15/jobshift/data/hf_cache/hub/models--BAAI--bge-m3
df -h .
```

目前此目錄尚不是 Git repo。開始改程式前執行：

```bash
git init -b feat/frus-agentic-rag
```

不要建立或直接修改 `main/master`。高容量資料全部加入 `.gitignore`。

已驗證可沿用的 WSL 資產：

- `jobshift:latest`：Python 3.12.13、uv、Torch 2.6.0+cu124、Sentence Transformers 3.4.1。
- image 已能看見 RTX 4060。
- BGE-M3 有兩份 cache；已實測可離線載入的是 `/home/haoche_nitro_v15/.cache/huggingface/hub`，約 4.3GB，`refs/main=5617a9f61b028005a4858fdac845db406aefb181`。
- WSL host 尚未安裝 uv；用 `jobshift:latest` 內的 uv 建立與鎖定專案，不需要污染 host Python。
- WSL／Windows 目前都未安裝 Ollama CLI；由 Compose 使用官方 Ollama container。

## 3. 建立現代 Python 專案

先定義暫時的 uv runner：

```bash
cd /home/haoche_nitro_v15/FRUS_agenticRAG
frus_uv() {
  docker run --rm \
    -v "$PWD:/workspace" -w /workspace \
    --entrypoint uv jobshift:latest "$@"
}
frus_uv init --package --python 3.12 --name frus-agentic-rag .
```

加入 dependencies：

```bash
frus_uv add \
  langgraph langgraph-checkpoint-sqlite langchain-core langchain-ollama ollama \
  lxml pyarrow lancedb sentence-transformers \
  fastapi "uvicorn[standard]" streamlit pydantic-settings orjson \
  typer rich httpx aiosqlite tenacity
frus_uv add --dev ruff mypy pytest pytest-asyncio pytest-cov pre-commit
```

在加入 Torch 前，於 `pyproject.toml` 沿用 Jobshift 已驗證的 CUDA source：

```toml
[[tool.uv.index]]
name = "pytorch-cu124"
url = "https://download.pytorch.org/whl/cu124"
explicit = true

[tool.uv.sources]
torch = [{ index = "pytorch-cu124" }]
```

然後：

```bash
frus_uv add "torch>=2.5,<3"
frus_uv lock
```

`pyproject.toml` 必須包含：

- `requires-python = ">=3.12,<3.13"`。
- console script：`frus = "frus_agentic_rag.cli:app"`。
- Ruff target `py312`、line length 100；lint `E,W,F,I,UP,B,SIM,RUF`。
- pytest strict config、`testpaths = ["tests"]`。
- mypy Python 3.12、`check_untyped_defs = true`。

目錄：

```text
src/frus_agentic_rag/
  config.py  cli.py
  ingest/{manifest,tei,chunk}.py
  index/{embed,build}.py
  retrieval/{models,lexical,dense,hybrid,tools}.py
  agent/{state,schemas,nodes,edges,graph,prompts}.py
  generation/{ollama_client,citations}.py
  api.py  ui.py
tests/{unit,integration,fixtures}/
eval/{gold_cases.jsonl,run_ablation.py}/
reports/
data/                         # gitignored
Dockerfile  compose.yaml  .env.example  README.md
```

## 4. Docker 與 GPU 分時

`Dockerfile` 先 `FROM jobshift:latest`，再以本專案的 `pyproject.toml`／`uv.lock` 執行 `uv sync --locked`。這是 MVP 的 layer reuse；README 必須說明，不把 Jobshift 描述成 FRUS 的必要依賴。

`compose.yaml` 至少提供：

- `dev`：FRUS image，掛載 source/data，執行測試與 CLI。
- `embed`：FRUS image、`gpus: all`、profile=`batch`，只跑離線 BGE-M3。
- `ollama`：`ollama/ollama`、`gpus: all`、named model volume。
- `api`：FastAPI；online BGE-M3 query embedding 預設 CPU，呼叫 `http://ollama:11434`。
- `ui`：Streamlit，呼叫 API。

BGE cache 必須把完整 `hub` 唯讀掛載，否則 snapshot 內相對 symlink 會斷裂：

```yaml
- /home/haoche_nitro_v15/.cache/huggingface/hub:/models/hf-hub:ro
```

啟動前讀取 `refs/main`、確認 snapshot 有 config/tokenizer/pooling/weight，再把 resolved path 寫入 `.env`；不要在程式永久寫死 snapshot hash。

離線 full embedding 前先停 Ollama，避免 8GB VRAM 競爭：

```bash
docker compose stop ollama
docker compose --profile batch run --rm embed frus embed --all --resume
docker compose up -d ollama
```

線上 Agent 使用 `qwen3:4b-instruct` Q4（約 2.5GB），`num_ctx=8192`、temperature=0、parallel=1。安裝：

```bash
docker compose up -d ollama
docker compose exec ollama ollama pull qwen3:4b-instruct
```

router、planner、grader 必須使用 Pydantic structured output；若 `ChatOllama.with_structured_output()` 不穩，改直接呼叫 Ollama JSON-schema `format`，不接受脆弱的 regex JSON parsing。

## 5. FRUS 來源與全量 ingest

來源 snapshot：

```text
/mnt/d/Project/sideProject/interview/frus-measurement
commit 4b4c402f0cce25144ded2198ba9566b6c37c7c49
```

先確認 WSL ext4 尚有至少 25GiB；有空間時將 `volumes/` 複製到 `data/raw/frus/volumes/`，避免長任務反覆跨 `/mnt/d` 讀取。複製後比對檔數與 checksum，不得刪除原 snapshot。

實作順序：

1. `frus manifest`：掃 694 XML；記 path、size、SHA-256、volume ID、source commit、status。
2. `lxml.etree.iterparse`：不以 regex 作 production parser。
3. 只有 `tei:div[@type='document'][@subtype='historical-document']` 進主 evidence corpus。
4. planned、總索引、front/back matter、persons/terms 分開保存。
5. 不跨 document 切塊；最多 512 BGE-M3 tokens、overlap 64。
6. per-volume Parquet、stable chunk ID、`.tmp` → checksum → atomic rename。
7. 先建全量 LanceDB BM25；dense 以 volume checkpoint 背景續跑。

驗收基準：694 XML、552 個含 document div 的 published volumes、314,483 個 `type=document` div；filter 後 historical-document 數可較少，但差異必須具名。

## 6. 先保留 2-step baseline

實作 `B0`：

```text
query → BM25 + BGE-M3 → RRF → top evidence → Qwen → citation gate
```

介面固定：

```python
async def hybrid_search(query: str, filters: SearchFilters, top_k: int = 10) -> list[Evidence]: ...
async def answer_2step(question: str, language: str) -> Answer: ...
```

後續 Agentic graph 必須使用同一個 retriever、generator、top-k 與資料版本，才能公平 ablation。

## 7. Walking skeleton：先用假工具跑通 Graph

在使用真索引前，建立 injectable fake planner／retriever／grader，跑通：

```text
START → plan → retrieve → grade → synthesize → citation_gate → END
                         ↘ rewrite → retrieve
                         ↘ abstain
```

至少測四條路：simple、complex multi-hop、一次 correction、unanswerable。Graph 必須 100% 在 budget 內終止，才准接真 BGE-M3／LanceDB。

## 8. Agent state、schemas 與工具

`AgentState` 至少包含：

```python
class AgentState(TypedDict):
    question: str
    answer_language: Literal["zh-TW", "en"]
    route: Literal["lookup", "simple", "timeline", "complex", "status"]
    subqueries: list[SubQuery]             # max 3
    evidence: list[Evidence]
    missing_hops: list[str]
    retrieval_rounds: int                 # max 2
    llm_calls: int                        # max 4
    draft_answer: str | None
    citation_errors: list[str]
    outcome: Literal["answer", "abstain"] | None
    trace: list[TraceEvent]
```

Pydantic schemas：

- `QueryPlan`：route、answer language、0–3 English subqueries、date/person/administration filters、required evidence slots。
- `EvidenceGrade`：每個 hop 為 `supported|partial|unsupported`、accepted evidence IDs、missing information、corrective query。
- `AgentAnswer`：claims、每 claim 的 evidence IDs、answer text、limitations。

只開放五個具 schema 的本機 tools：

1. `hybrid_search(query, filters, top_k)`。
2. `lookup_document(volume_id, document_id)`。
3. `timeline_search(query, date_from, date_to, filters)`。
4. `get_adjacent_context(chunk_id)`。
5. `get_series_status(volume_or_period)`。

LLM 不得產生 SQL、檔案路徑或任意 URL；canonical URL 必須由程式組合。

## 9. LangGraph nodes 與 conditional edges

依序實作：

1. `plan_query`：factual/history 問題一律進 FRUS tools；只有問候／系統使用說明可不檢索。
2. `dispatch_retrieval`：依 route 選工具；complex 的 subqueries 可平行執行。
3. `merge_evidence`：按 document/chunk 去重、RRF、保留每個 hop 的 provenance。
4. `grade_evidence`：只判 accepted evidence 是否覆蓋 required hops，不得補寫史實。
5. `rewrite_missing`：只重寫 missing hop，保留人物／年代約束；最多一次。
6. `synthesize`：只能使用 accepted evidence；衝突文件並列，不替史家裁決。
7. `validate_citations`：deterministic 檢查 evidence ID、volume/document、section type、canonical URL。
8. `abstain`：citation 失敗直接拒答；citation block 可由程式重建，不再花一次 LLM call 修網址。

使用 `langgraph-checkpoint-sqlite` 保存 thread state；state 只存 evidence IDs 與摘要，不把整批 chunk text 重複塞入每個 checkpoint。

每個 node 記錄：開始／結束時間、input/output schema version、LLM/retrieval calls、token count、route、error。UI 顯示 graph path、subqueries、tool calls、accepted evidence、correction 與引用。

## 10. Agentic kill-test

在接全量前建立 30 個 gold semantic cases，中文／英文各跑一次，共 60 runs：

- 10 single-document lookup。
- 10 multi-document comparison／timeline／causal questions；每題 2–4 gold documents。
- 5 初次用詞不匹配、預期 correction 命中的問題。
- 5 unanswerable／planned／越界問題。

Ablation：

- `B0`：2-step hybrid。
- `B1`：B0 + planning/decomposition。
- `B2`：B1 + evidence grade/rewrite/abstain。
- `B3`：B2 + citation gate/repair，完整系統。

必報：All-Evidence Recall@10、answer correctness、route macro-F1、correction gain、abstention precision/recall、citation precision、claim citation coverage、LLM/retrieval calls、p50/p95 latency、peak VRAM。

預登記門檻：

1. B3 在 multi-hop All-Evidence Recall 或 answer correctness 未比 B0 高至少 10 percentage points，README 不得宣稱 Agentic 提升；預設退回有實證的最小版本。
2. route macro-F1 < 0.80，或 simple 題 >20% 被多餘拆解，改 rule-first router；LLM 只產生 subqueries。
3. 任何 unanswerable 題產生無證據的肯定史實，grader 不得單獨放行；改 required-hop coverage + score threshold + grade 聯合 gate。
4. 任何不存在、未檢索或非 canonical citation 是阻斷失敗；repair 或 abstain。
5. B3 p95 >30 秒或超過 B0 2.5 倍，subqueries 3→2，保留一次 evidence correction；仍失敗則 UI 預設 B1。
6. 10 個 semantic intents 各做 3 個同義改寫；route/tool path consistency <80%，改成 deterministic/rule-first route。不要用更大 recursion limit掩蓋不穩定。

## 11. BGE-M3 與生成模型 gates

BGE-M3 先跑最終設定的 10k chunks benchmark：batch 16 起，OOM 時 16→8→4；記 chunks/s、tokens/s、peak VRAM、Parquet throughput。全量估計 65–100 萬 chunks，8 小時 dense 需約 23–35 chunks/s。

若 BGE-M3 過慢：保留全量 BM25，改測 `intfloat/multilingual-e5-small`；用同一 10k sample 比速度與 Recall@10，不先重做全庫。

已實測 BGE-M3 單次結果：GPU 2 queries encode 0.941 秒、peak 2.125GiB；CPU 2 queries 0.428 秒。樣本太小，線上仍須跑 warm 30-query p95，但第一版採 Qwen GPU、BGE query CPU。

Qwen3 4B gate：以 40 個中英文 planner/grader cases 測 structured-output valid rate、tool/route accuracy、latency。valid rate <95% 時依序：

1. 縮短 schema／prompt，temperature=0。
2. rule-first router，Qwen 只做 decomposition／grade。
3. 才測 `qwen3:8b`；若 VRAM／p95 不過門檻，回到 4B bounded graph，不硬升模型。

## 12. 實作順序

嚴格依下列 gate，不要同時開工：

1. 專案骨架、Docker、quality tools。
2. fake tools walking skeleton + graph termination tests。
3. 3-volume TEI parse + BM25 + BGE dense。
4. B0 2-step baseline + canonical citations。
5. LangGraph B1 planner/decomposition。
6. B2 grade/rewrite/abstain。
7. B3 citation gate/repair + SQLite checkpoint。
8. 30 semantic cases × bilingual ablation；決定保留哪些 nodes。
9. 全量 manifest/parse/BM25。
10. 10k BGE benchmark → checkpointed full dense。
11. FastAPI、Streamlit trace UI、README、demo。

## 13. 必跑指令與交付

```bash
docker compose build
docker compose run --rm dev uv run ruff format --check .
docker compose run --rm dev uv run ruff check .
docker compose run --rm dev uv run mypy src
docker compose run --rm dev uv run pytest --cov=frus_agentic_rag

docker compose run --rm dev uv run frus graph-smoke
docker compose run --rm dev uv run frus ingest --limit-volumes 3
docker compose run --rm dev uv run frus benchmark --chunks 10000
docker compose run --rm dev uv run frus eval --systems B0,B1,B2,B3
```

完成回報只交：

1. 實跑輸出與失敗項目。
2. `reports/corpus_stats.json`、`benchmark.json`、`agent_ablation.json`、`route_stability.json`。
3. BM25 coverage `552/552`；dense coverage `X/552` 與實測 ETA。
4. B0–B3 品質、延遲、LLM calls 比較，以及哪些 nodes 因 kill-test 被移除。
5. API/UI 啟動指令、URL、graph screenshot、三個可重現 demo questions。
6. 已知 UNKNOWN：Qwen router/grader accuracy、全量 build time、agentic quality gain；沒有實測前不得寫成已完成。

## 14. 主要依據

- [LangGraph 官方 Agentic RAG 教程](https://docs.langchain.com/oss/python/langgraph/agentic-rag)
- [LangGraph overview／deterministic + agentic workflow](https://docs.langchain.com/oss/python/langgraph/overview)
- [LangGraph persistence](https://docs.langchain.com/oss/python/langgraph/persistence)
- [Adaptive-RAG, NAACL 2024](https://aclanthology.org/2024.naacl-long.389/)
- [Corrective RAG, 2024](https://arxiv.org/abs/2401.15884)
- [MultiHop-RAG, COLM 2024](https://arxiv.org/abs/2401.15391)
- [SELF-RAG, ICLR 2024](https://proceedings.iclr.cc/paper_files/paper/2024/file/25f7be9694d7b32d5cc670927b8091e1-Paper-Conference.pdf)
- [Adaptive RAG query-variation robustness, 2026](https://arxiv.org/abs/2604.10745)
- [Ollama structured outputs](https://docs.ollama.com/capabilities/structured-outputs)
- [Ollama tool calling](https://docs.ollama.com/capabilities/tool-calling)
