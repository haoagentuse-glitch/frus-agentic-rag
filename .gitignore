.env
.env.local
data/
# reports/*.json are tracked on purpose: they are the evidence for every number
# the README claims. Only the raw per-run dump and logs are too noisy to keep.
reports/ablation_runs.jsonl
reports/*.log
*.lance/
*.parquet
.venv/
.uv-cache/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/
.ollama/
checkpoints/*.sqlite
scratch/
.gcp_zone
